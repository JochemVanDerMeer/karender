function printToConsole(s) {
    console.log(s);
}
printToConsole("test");
var testing = [1, 2, 3];
function testingout(s) {
    var test = "";
    if (typeof (test) === 'number') {
        return "";
    }
    else {
        return "yes";
    }
}
